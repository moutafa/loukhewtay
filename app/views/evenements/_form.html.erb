<%= form_for(@evenement) do |f| %>
  <% if @evenement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@evenement.errors.count, "error") %> prohibited this evenement from being saved:</h2>

      <ul>
      <% @evenement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :rows=>5 %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :frequence %><br />
    <%= f.text_field :frequence %>
  </div>
  <div class="field">
    <%= f.label :confidentialite %><br />
    <%= f.text_field :confidentialite %>
  </div>
  <div class="field">
    <%= f.label :endroit_id %><br />
    <%= f.collection_select(:endroit_id, Endroit.all, :id, :nom) %>
  </div>
  <div class="field">
    <%= f.label :categorie_Event %><br />
    <%= f.collection_select(:categorieEvent_id, CategorieEvent.all, :id, :libelle) %>
  </div>
  <div class="field">
    <%= f.hidden_field :user_id, :value=>session[:user_id] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
